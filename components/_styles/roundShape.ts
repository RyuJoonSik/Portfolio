import { css } from "styled-components";

const mediumSize = "44px";

const roundShape = {
  medium: css`
    width: ${mediumSize};
    height: ${mediumSize};
    border-radius: 50%;
  `,
};

export default roundShape;
