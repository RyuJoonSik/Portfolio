import { css } from "styled-components";

/**
 * 폰트 굵기를 설정합니다.
 */
const fontWeight = {
  /**
   * ```css
   * font-weight: 400;
   * ```
   */
  regular: css`
    font-weight: 400;
  `,
  /**
   * ```css
   * font-weight: 500;
   * ```
   */
  medium: css`
    font-weight: 500;
  `,
  /**
   * ```css
   * font-weight: 700;
   * ```
   */
  bold: css`
    font-weight: 700;
  `,
};

export default fontWeight;
