import styled from "styled-components";

import BOX_STYLE from "./_mixins";

const MainFullContentItemContainer = styled.div`
  ${BOX_STYLE.TYPE.MAIN_CONTENT_ITEM_CONTAINER};
  width: 100%;
`;

export default MainFullContentItemContainer;
