import { css } from "styled-components";

const SMALLEST_SIZE = "0.3rem";
const SMALLER_SIZE = "0.6rem";

export const PADDING_VERTICAL_SMALLEST = css`
  padding: ${SMALLEST_SIZE} 0;
`;

export const PADDING_HORIZONTAL_SMALLEST = css`
  padding: 0 ${SMALLEST_SIZE};
`;

export const PADDING_VERTICAL_SMALLER = css`
  padding: ${SMALLER_SIZE} 0;
`;

export const PADDING_HORIZONTAL_SMALLER = css`
  padding: 0 ${SMALLER_SIZE};
`;
